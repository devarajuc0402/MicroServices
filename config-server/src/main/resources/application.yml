server:
  port: 8080

spring:
  application:
    name: config-server

  cloud:
   config:
    server:
      git:
        uri: file:///C:/Users/Smile/Desktop/Java_21/Workspace_Microservices/config-repository
        default-label: master

  profiles:
    active: config-server

management:
  endpoints:
    web:
      exposure:
        include: health,info